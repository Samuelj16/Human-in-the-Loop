# API
fastapi[standard]==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# storage
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
aiosqlite==0.22.1           # local fallback only

# auth
python-jose[cryptography]==3.5.0
bcrypt==5.0.0

# config
pydantic-settings==2.15.0
alembic==1.19.1             # schema migrations (authoritative in production)

# model providers (one adapter each, never mixed)
anthropic==1.0.0
openai==3.3.1

# search + jobs
httpx==0.28.1
arq==0.28.0
redis==5.3.1

# reports
markdown==3.10.3
weasyprint==69.0            # needs pango/cairo at runtime

# dev
pytest==9.1.1
pytest-asyncio==1.4.0
