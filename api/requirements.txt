# API
fastapi[standard]==0.141.1
uvicorn[standard]==0.41.0
python-multipart==0.0.20

# storage
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
aiosqlite==0.21.0          # local fallback only

# auth
python-jose[cryptography]==3.5.0
bcrypt==5.0.0

# config
pydantic-settings==2.13.1

# model providers (one adapter each, never mixed)
anthropic==1.0.0
openai==3.3.1

# search + jobs
httpx==0.29.2
arq==0.28.0
redis==7.1.0

# reports
markdown==3.10.2
weasyprint==69.0            # needs pango/cairo at runtime

# dev
pytest==9.0.2
pytest-asyncio==1.3.0
